machine:
  node:
    version: 6.10.2
dependencies:
  cache_directories:
    - bower_components
deployment:
  production:
    branch: master
    commands:
      - make deploy-by-day
notify:
  webhooks:
    - url: 'https://ft-next-webhooks.herokuapp.com/circleci'
